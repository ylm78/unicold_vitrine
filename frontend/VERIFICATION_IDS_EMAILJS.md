# ✅ Vérification des IDs EmailJS

## 🔍 Problème : "The service ID not found"

L'erreur indique que le Service ID utilisé n'existe pas dans votre compte EmailJS.

---

## ✅ IDs Corrects à Utiliser

### Pour les Devis (devis@unicold.fr)
- **Service ID** : `service_ixgvh57`
- **Template ID** : `template_57qr94g`

### Pour les Visites Showroom (contact@unicold.fr)
- **Service ID** : `service_ugehbqe`
- **Template ID** : `template_u2iy10q`

---

## 🔧 Vérification dans EmailJS

### 1. Vérifier que les Services Existent

1. Allez sur https://dashboard.emailjs.com/
2. Cliquez sur **"Email Services"**
3. Vérifiez que vous avez bien :
   - `service_ixgvh57` (pour devis@unicold.fr)
   - `service_ugehbqe` (pour contact@unicold.fr)

**Si un service n'existe pas :**
- Créez-le ou utilisez un service existant
- Notez le Service ID exact

### 2. Vérifier que les Templates Existent

1. Allez dans **"Email Templates"**
2. Vérifiez que vous avez bien :
   - `template_57qr94g` (pour devis@unicold.fr)
   - `template_u2iy10q` (pour contact@unicold.fr)

**Si un template n'existe pas :**
- Créez-le ou utilisez un template existant
- Notez le Template ID exact

---

## 📝 Action Immédiate

**Donnez-moi les IDs EXACTS que vous voyez dans EmailJS :**

1. **Service ID pour devis@unicold.fr** : `service_???????`
2. **Template ID pour devis@unicold.fr** : `template_???????`
3. **Service ID pour contact@unicold.fr** : `service_???????`
4. **Template ID pour contact@unicold.fr** : `template_???????`

Une fois que vous me donnez ces IDs, je mettrai à jour le code avec les bons identifiants.

---

## 🧪 Test Rapide

Pour vérifier qu'un Service ID existe, allez dans EmailJS → Email Services et regardez la liste. Les IDs sont affichés à côté de chaque service.

